mixin popup(data)
    +b.popup.d-flex.jc-center()&attributes(attributes)
        +b.header.__header
            +e.IMAGE.close(src="%=static=%img/content/close.png")
        +b.FORM.form.__form
            +e.H2.title Обратная связь
            +e.P.text Заполните форму и наш менеджер свяжется с вами в течении 10 минут
            +e.input-container
                +e.INPUT.input(placeholder="Ваше имя" name="name" type="text" required minlength='3' title="Зполните ваше имя правильно")
            +e.input-container
                +e.INPUT.input(placeholder="Телефон" name="phone" id='mask' type="tel" pattern="^((8|\\+7)[\\- ]?)?(\\(?\\d{3}\\)?[\\- ]?)?[\\d\\- ]{7,10}$" title='+79261234567' required)
            +e.BUTTON.btn-send(type='submit') Отправить
            +e.LABEL.label-container
                +e.INPUT.checkbox(type="checkbox" name="checkbox-test" required )
                +e.SPAN.checkbox-custom
                +e.SPAN.small-text Нажимая кнопку вы соглашаетесь с политикой конфиденциальности

    +b.overlay

